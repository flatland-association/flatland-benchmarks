{
  "name": "backend",
  "version": "0.0.0",
  "description": "FAB backend",
  "scripts": {
    "serve": "npm run private:build && cd ../../out-tsc && mkdir backend && cp -r backend-build/app backend && mkdir backend/config && cp -n ../ts/backend/src/config/config.jsonc \"$_\" && cd backend/app && node main.min.js",
    "build": "npm run private:build && mkdir -p ../../dist/backend/config && cp -r ../../out-tsc/backend-build/app ../../dist/backend",
    "docker:rabbitmq": "docker run -d --name amqp.test -p 5672:5672 rabbitmq",
    "private:build": "tsc && npx webpack"
  }
}
