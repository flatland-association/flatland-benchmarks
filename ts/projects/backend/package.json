{
  "name": "backend",
  "version": "0.0.0",
  "description": "FAB backend",
  "scripts": {
    "serve": "tsc --project tsconfig.development.json && cd out-tsc/backend/app && node main.js",
    "build": "tsc",
    "docker:rabbitmq": "docker run -d --name amqp.test -p 5672:5672 rabbitmq"
  },
  "dependencies": {
    "amqplib": "^0.10.5",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.6",
    "@types/express": "^5.0.0"
  }
}
